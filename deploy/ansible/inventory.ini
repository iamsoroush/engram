# Inventory for the Engram production host. Uses your ~/.ssh/config `engram` alias, user `ubuntu`.
#   ansible-playbook -i deploy/ansible/inventory.ini deploy/ansible/setup-production.yml
[prod]
engram ansible_user=ubuntu
