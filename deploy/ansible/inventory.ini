# Sample inventory for deploy/ansible/prepare-server.yml — copy/edit for your server.
#   ansible-playbook -i deploy/ansible/inventory.ini deploy/ansible/prepare-server.yml
[engram]
engram-prod ansible_host=YOUR_VPS_IP ansible_user=root

[engram:vars]
# ssh_port=22
# docker_registry_mirror=https://<your-iranian-docker-hub-mirror>
# harden_ssh=true            # only once key-based SSH login works
